# 🎨 Web Interface Variants / Варианты веб-интерфейса

Selkies GStreamer теперь поддерживает два варианта веб-интерфейса:

## 📦 Доступные варианты

### 1. gst-web-react (по умолчанию) ✨

**Современная версия на React + TypeScript**

- 🔧 Технологии: React 18, TypeScript 5, Vite 5
- 📘 Полная типизация
- 🐛 Встроенный режим отладки
- ⚙️ Гибкая конфигурация через URL/props
- 📊 Расширенный мониторинг производительности

**Размещение:** `addons/gst-web-react/`

### 2. gst-web (оригинал)

**Проверенная версия на Vue.js**

- 🔧 Технологии: Vue.js 2, JavaScript
- 📦 Компактный размер
- ⚡ Быстрая сборка
- 🔄 Обратная совместимость

**Размещение:** `addons/gst-web/`

---

## 🚀 Использование

### Сборка с выбором варианта

```bash
# По умолчанию - gst-web-react (React + TypeScript)
./build.sh

# Явно указать gst-web-react
WEB_VARIANT=gst-web-react ./build.sh

# Использовать оригинальный gst-web (Vue.js)
WEB_VARIANT=gst-web ./build.sh
```

### Результат сборки

Независимо от выбранного варианта, результат будет:

```
dist/selkies-gstreamer-web_v{VERSION}.tar.gz
```

Внутри архива:
- `gst-web-react/` (для React версии)
- `gst-web/` (для Vue.js версии)

---

## 📊 Сравнение

| Характеристика | gst-web-react | gst-web |
|---------------|---------------|---------|
| **Фреймворк** | React 18 | Vue.js 2 |
| **Язык** | TypeScript | JavaScript |
| **Типизация** | ✅ Полная | ❌ Нет |
| **Размер bundle** | ~190 KB | ~150 KB |
| **Время сборки** | ~30 сек | ~20 сек |
| **Режим отладки** | ✅ Встроен | ⚠️ Базовый |
| **Конфигурация** | URL + Props | Hostname |
| **Монолитный компонент** | ✅ Да | ❌ Нет |
| **Dev сервер** | Vite (HMR) | Webpack |
| **Обратная совместимость** | ⚠️ Новый | ✅ Полная |

---

## 💡 Когда использовать

### gst-web-react (рекомендуется для новых проектов)

✅ Новые проекты  
✅ Нужна полная типизация  
✅ Планируется кастомизация  
✅ Нужен режим отладки  
✅ Разработка на TypeScript  
✅ Интеграция в React приложения  

### gst-web (для существующих проектов)

✅ Существующие развертывания  
✅ Минимальный размер bundle критичен  
✅ Быстрая сборка важна  
✅ Обратная совместимость необходима  
✅ Работающие Vue.js интеграции  

---

## 📖 Документация

### gst-web-react

- [README.md](addons/gst-web-react/README.md) - основная документация
- [DEPLOY.md](addons/gst-web-react/DEPLOY.md) - развертывание на сервере
- [QUICK_START_RU.md](addons/gst-web-react/QUICK_START_RU.md) - быстрый старт на русском
- [BUILD_INSTRUCTIONS.md](addons/gst-web-react/BUILD_INSTRUCTIONS.md) - инструкции по сборке

### gst-web

- [README](addons/gst-web/README) - основная документация
- См. также основную документацию Selkies

---

## 🔄 Миграция

### С gst-web на gst-web-react

1. **Пересоберите с новым вариантом:**
   ```bash
   WEB_VARIANT=gst-web-react ./build.sh
   ```

2. **Обновите развертывание:**
   ```bash
   sudo tar -xzf dist/selkies-gstreamer-web_v*.tar.gz -C /opt
   sudo cp -r /opt/gst-web-react/* /var/www/gst-web/
   ```

3. **Обновите конфигурацию (если нужно):**
   - URL параметры работают одинаково
   - Добавлен параметр `debug=true` для режима отладки
   - Props теперь поддерживаются (для React интеграций)

### С gst-web-react на gst-web

1. **Пересоберите с оригинальным вариантом:**
   ```bash
   WEB_VARIANT=gst-web ./build.sh
   ```

2. **Обновите развертывание:**
   ```bash
   sudo tar -xzf dist/selkies-gstreamer-web_v*.tar.gz -C /opt
   sudo cp -r /opt/gst-web/* /var/www/gst-web/
   ```

---

## 🎯 Особенности gst-web-react

### 1. Режим отладки

```
http://your-server.com/?debug=true
```

Показывает в реальном времени:
- 📊 Битрейт видео/аудио
- 🎬 FPS
- ⏱️ Задержка
- 💻 Загрузка CPU/GPU
- 🧠 Использование памяти
- 📐 Разрешение видео
- 🎨 Используемый кодек

### 2. Гибкая конфигурация

**Через URL:**
```
?server=111.111.111.111&port=8080&app=desktop&debug=true&encoder=h264
```

**Через props (для интеграции):**
```tsx
<App config={{
  server: '111.111.111.111',
  port: 8080,
  appName: 'desktop',
  debug: true
}} />
```

### 3. TypeScript интеграция

```typescript
import { App, ConnectionConfig } from './App';

const config: ConnectionConfig = {
  server: 'my-server.com',
  port: 8080,
  appName: 'desktop'
};

<App config={config} />
```

---

## 🔧 Разработка

### gst-web-react

```bash
cd addons/gst-web-react
npm install
npm run dev  # http://localhost:5173
```

### gst-web

```bash
cd addons/gst-web
npm install
npm run dev  # Следуйте инструкциям
```

---

## 🐛 Отладка

### Проверить какой вариант используется

```bash
# Посмотреть содержимое архива
tar -tzf dist/selkies-gstreamer-web_v*.tar.gz | head -5

# gst-web-react покажет:
# gst-web-react/
# gst-web-react/index.html
# gst-web-react/assets/

# gst-web покажет:
# gst-web/
# gst-web/index.html
# gst-web/js/
```

### Проверить в браузере

Откройте Developer Tools (F12) → Console:

**gst-web-react:**
```
Built with: React + TypeScript + Vite
```

**gst-web:**
```
Vue.js detected
```

---

## 📞 Поддержка

Если возникли вопросы:

- **Для gst-web-react:** создайте issue с тегом `gst-web-react`
- **Для gst-web:** создайте issue с тегом `gst-web`
- **Общие вопросы:** основная документация Selkies

---

## 🎉 Рекомендация

**Для новых проектов рекомендуется использовать gst-web-react (по умолчанию).**

Он предоставляет лучший опыт разработки, полную типизацию и расширенные возможности отладки, при этом сохраняя полную функциональную совместимость с оригиналом.

---

**Версия документа:** 1.0  
**Последнее обновление:** 2025-11-16

